import { makeStyles } from 'tss-react/mui';

export default makeStyles()((theme) => ({
    title: {
        display: 'flex',
        margin: theme.spacing(2),
    },
    weight: {
        fontWeight: '500',
    }
}))