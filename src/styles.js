import { makeStyles } from 'tss-react/mui';

export default makeStyles()((theme) => ({
    app: {
        display: "flex",
        width: '100%',
        height: '100vh',
        overflowY: 'auto'
    }
}))